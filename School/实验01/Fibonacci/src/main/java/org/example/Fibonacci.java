package org.example;

public class Fibonacci {
    // 实验：输出Fibonacci数列的前num项
    // 如果num小于零，则返回null;
    // 否则返回一维数组表示其前num项。
    public static int[] compute(int num) {
        if (num <= 0) {
            return null;
        } else {
            // 在这里补充缺少的代码：
            return null;
        }
    }
}
